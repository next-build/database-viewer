/**
 * Accept a string/object
 * and resolve it into the plugin format above
 * @param item
 * @returns {*}
 */
export function resolvePlugin(item: any): any;
/**
 * Load a plugin from disk
 * @param item
 * @returns {*}
 */
export function requirePlugin(item: any): any;
